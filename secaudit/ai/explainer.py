"""AI-powered vulnerability explainer.

Uses an external LLM (via SECAUDIT_AI_KEY) to provide
human-readable explanations and fix suggestions for detected issues.

This module is entirely optional and never blocks scanning.
"""
